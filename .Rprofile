source("renv/activate.R")

# Allow absolute module imports (relative to the project root).
options(box.path = getwd())

options(repos = c(
  "https://cabbage.tools:4243/prod-cran/latest",
  "https://cabbage.tools:4243/prod-git/latest",
  "https://cabbage.tools:4243/prod-contrib/latest"
))
